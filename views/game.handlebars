<div class="row">
	<div class="backPic2 col-xs-12 {{roomClass}}">
		<div class="animated fadeIn" id="slowFade">
			<div class="frame col-xs-12 {{frameClass}}">
				<div class="col-xs-4 col-xs-offset-5">
					<h1>{{name}}</h1>
				</div>
				<div class="gameTwo col-xs-10 col-xs-offset-1">
		        	<span style="color: white;">{{text}}</span>
				</div>
				<div class="btn btn-default col-xs-3 col-xs-offset-3" value="{{button1}}">{{button1}}</div>
				<div class="btn btn-default col-xs-3" value="{{button2}}">{{button2}}</div>
		    </div>
		</div>
	</div>
</div>