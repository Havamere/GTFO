<div class="row">
	<div class="backPic2 {{roomClass}}">
		<div class="col-xs-2 col-xs-offset-5">
			<h1>{{name}}</h1>
		</div>
		<div class="animated fadeIn" id="slowFade">
			<div class="frame col-xs-10 col-xs-offset-1  {{frameClass}}">
				<div class="gameTwo col-xs-10 col-xs-offset-1">
		        	<span style="color: white;">{{text}}</span>
				</div>
				<div class="btn btn-default col-xs-5 col-xs-offset-1" value="{{button1}}">{{button1}}</div>
				<div class="btn btn-default col-xs-5 col-xs-offset-1" value="{{button2}}">{{button2}}</div>
		    </div>
		</div>
	</div>
</div>